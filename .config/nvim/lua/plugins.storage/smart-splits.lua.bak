return {
    "mrjones2014/smart-splits.nvim",
    enabled = false,
    opts = {},
    config = function(_, opts)
        require("smart-splits").setup(opts)

        vim.keymap.set("n", "<C-h>", require("smart-splits").move_cursor_left, { nowait = true })
        vim.keymap.set("n", "<C-j>", require("smart-splits").move_cursor_down, { nowait = true })
        vim.keymap.set("n", "<C-k>", require("smart-splits").move_cursor_up, { nowait = true })
        vim.keymap.set("n", "<C-l>", require("smart-splits").move_cursor_right, { nowait = true })
        -- vim.keymap.set("n", "<C-\\>", require("smart-splits").move_cursor_previous)

        -- -- these keymaps will also accept a range,
        -- -- for example `10<A-h>` will `resize_left` by `(10 * config.default_amount)`
        -- vim.keymap.set("n", "<A-h>", require("smart-splits").resize_left)
        -- vim.keymap.set("n", "<A-j>", require("smart-splits").resize_down)
        -- vim.keymap.set("n", "<A-k>", require("smart-splits").resize_up)
        -- vim.keymap.set("n", "<A-l>", require("smart-splits").resize_right)

    end
}
